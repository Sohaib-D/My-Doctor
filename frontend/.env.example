# Optional in production (same-origin FastAPI serving).
# Use this only if frontend is served from a separate dev host.
# Backend AI/Groq configuration lives in root .env.example (project root).
# Frontend only needs VITE_API_URL.
VITE_API_URL=http://127.0.0.1:8000
